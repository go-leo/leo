package app

import (
	"github.com/gin-gonic/gin"
)

func Route(rg *gin.RouterGroup) {
	//router := rg.Group("/app")
	//router.GET("/version", func(c *gin.Context) {
	//	c.IndentedJSON(http.StatusOK, global.AppVersion())
	//})
}
